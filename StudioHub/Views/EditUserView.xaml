<ui:FluentWindow x:Class="StudioHub.Views.EditUserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:a="clr-namespace:StudioHub"
    xmlns:h="clr-namespace:StudioHub.Helpers"
    xmlns:v="clr-namespace:StudioHub.Views"
    xmlns:vm="clr-namespace:StudioHub.ViewModels"
    Height="600" Width="800"
    FontFamily="Segoe UI Variable Display, Segoe UI"
    TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType"
    TextOptions.TextHintingMode="Fixed"
    ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica"
    SnapsToDevicePixels="True" UseLayoutRounding="True"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    d:DataContext="{d:DesignInstance Type=vm:BaseViewModel,
                                     IsDesignTimeCreatable=True}"
    >
    <Window.Resources>
        <DataTemplate x:Key="AvatarItemTemplate">
            <StackPanel Margin="2" Orientation="Horizontal">
                <Image
                    Source="{Binding IsSaved, Converter={h:InverseBooleanConverter}}"
                    Height="24" Width="24"
                    Margin="0 0 10 0"
                    RenderOptions.BitmapScalingMode="HighQuality"
                    />
                <TextBlock
                    Text="{Binding}"
                    VerticalAlignment="Center"
                    Foreground="White"
                    />
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <DockPanel LastChildFill="True">

        <ui:TitleBar Title="Modifica Profilo Utente" DockPanel.Dock="Top">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="{x:Null}"/>
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <StackPanel
            Margin="25"
            HorizontalAlignment="Right"
            DockPanel.Dock="Bottom" Orientation="Horizontal"
            >
            <ui:Button
                Content="Salva"
                MinWidth="100"
                Margin="0 0 15 0"
                Appearance="Primary"
                Command="{Binding SaveCommand}"
                />
            <ui:Button
                Content="Annulla"
                MinWidth="100"
                Appearance="Secondary"
                Command="{Binding CancelCommand}"
                />
        </StackPanel>

        <Grid Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

        </Grid>

    </DockPanel>
</ui:FluentWindow>
